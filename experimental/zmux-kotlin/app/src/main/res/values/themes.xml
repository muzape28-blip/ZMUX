<resources>
    <style name="Theme.Zmux" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/zmux_bg</item>
        <item name="android:statusBarColor">@color/zmux_surface</item>
        <item name="android:navigationBarColor">@color/zmux_surface</item>
        <item name="android:colorBackground">@color/zmux_bg</item>
    </style>

    <!-- Cold-start splash: pure black, no chrome, so the cmatrix rain owns the screen. -->
    <style name="Theme.Zmux.Splash" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
    </style>
</resources>
